class Employee {
    constructor(name, id, title) {
        this.name = name;
        this.id = id;
        this.title = title;
    }
    getName() {
        // Grab name from inquirer
    }
    getId() {
        // Grab ID from inquirer
    }
    getEmail() {
        // Grab Email from inquirer
    }
    getRole() {
        return 'Employee'
    }
}